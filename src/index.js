import React from "react";
import ReactDOM from 'react-dom';

import App from './App';
      //pass the component  //trigger the component
ReactDOM.render(<App />, document.getElementById('root'));

